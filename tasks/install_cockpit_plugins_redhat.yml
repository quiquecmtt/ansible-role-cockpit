---
- name: Ensure Cockpit plugins are installed
  become: true
  tags:
    - cockpit
    - cockpit_plugins
  ansible.builtin.dnf:
    name: "{{ cockpit_plugins }}"
    state: present
    update_cache: true
  when: ansible_os_family == "RedHat"
